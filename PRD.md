Nyx 项目背景与第一阶段大致方案

1. 项目背景

Nyx 是一个首先面向自己使用的桌面 AI 工具项目。

它的起点不是商业化，也不是先定义一个面向大众的完整产品，而是基于一个非常明确的现实动机：
	•	我长期在使用 ChatGPT、Claude、Codex 这一类工具
	•	市面上的现有产品虽然可用，但总会有一些不符合我个人习惯、工作流和审美的地方
	•	我希望做一个真正贴合自己使用方式的桌面 AI 工具
	•	我也希望借这个项目系统性熟悉相关技术，包括 Electron、桌面端架构、AI 聊天客户端、后续的 Agent / Skill 扩展能力，以及更严格的 TypeScript、函数式建模与测试方法

因此，Nyx 的第一阶段不是“做一个产品”，而是“做一个自己愿意长期使用的工具”。

如果后续这个工具真的足够好用、足够稳定、足够有特色，再考虑逐步给其他人使用，并进一步演进为更完整的产品。

⸻

2. 项目目标

2.1 当前目标

第一阶段的目标很明确：

在几周内做出一个可用、可持续迭代、自己愿意真正使用的桌面 AI 聊天工具。

它至少应该具备：
	•	与 AI 的普通聊天能力
	•	多会话管理
	•	本地持久化
	•	基础设置能力
	•	可扩展的架构基础

2.2 中长期方向

在当前阶段，不详细展开二三阶段设计，只保留方向感：
	•	后续可以扩展成支持更多模型和更复杂的对话能力
	•	可以逐步支持 Skill / Tool 调用
	•	未来可以探索 Agent 化能力
	•	如果确实做得足够好，再考虑开放给其他人使用

当前的核心要求不是提前设计完整未来，而是：

第一阶段的设计不要把后续道路堵死。

⸻

3. 为什么现在值得做

这个项目现在开始做，是合理的，原因主要有四个：

3.1 有真实需求

Nyx 不是为了“找一个项目练手”而硬造出来的，而是因为现有 AI 工具确实无法完全满足自己的使用方式。

这种“先给自己用”的项目，最大的优点就是需求真实、反馈真实、打磨方向明确。

3.2 和个人能力模型匹配

这个项目和当前的技术背景高度匹配：
	•	熟悉前端工程与桌面端技术栈
	•	对工具类产品有较强的判断力和审美
	•	对 AI、Agent、Skill、开发提效这条路线本来就长期感兴趣
	•	希望借一个实际项目来沉淀更先进的工程方法

3.3 可以作为长期实验田

即便这个项目最终没有商业化，也依然有明显价值：
	•	能得到一个自己真正会用的工具
	•	能训练一套高质量的工程实践
	•	能为未来更复杂的 AI 工具、Agent 产品或副业方向打基础

3.4 有利于控制复杂度

因为第一阶段只面向自己，不追求盈利、不追求大而全，所以可以更理性地控制 scope，避免一开始把项目做成“半个操作系统”。

⸻

4. 当前设计原则

基于目前讨论，Nyx 第一阶段建议遵循以下原则。

4.1 第一阶段优先，不提前过度设计未来

二三阶段只保留方向感，不做过细设计。

当前只要求：
	•	架构不要明显阻碍未来扩展
	•	但不要为了未来可能发生的事情，提前做大量抽象

4.2 Functional Core, Imperative Shell

函数式思想主要放在核心模型与核心逻辑层，而不是强行覆盖整个项目。

也就是说：
	•	核心状态迁移、数据变换、协议建模尽量纯函数化
	•	Electron、IPC、数据库、文件系统、窗口管理等边界层允许务实处理
	•	通过分层来控制副作用，而不是幻想整个桌面应用没有副作用

4.3 高质量 TypeScript

项目会采用：
	•	最新稳定版 TypeScript
	•	尽可能严格的 tsconfig 配置
	•	显式的类型建模
	•	更强的状态约束和错误建模

目标不是做类型体操，而是提升正确性、可维护性和可演进性。

4.4 测试不是附属品，而是工程的一部分

第一阶段就会引入：
	•	单元测试
	•	契约测试
	•	集成测试
	•	少量关键 E2E
	•	property-based testing（如 fast-check）

测试的目标不是“覆盖率好看”，而是让核心逻辑和边界契约真正可靠。

4.5 不追求教条式“纯函数式”

会优先吸收函数式设计思想，例如：
	•	不可变数据
	•	纯函数状态迁移
	•	显式错误建模
	•	discriminated union
	•	schema-first
	•	输入输出清晰

但不会为了“形式纯度”牺牲开发速度和可落地性。

⸻

5. 第一阶段的大致方案

5.1 目标定义

Nyx v0 的定位：

一个给自己长期使用的桌面 AI 聊天工具。

不是：
	•	多 Agent 平台
	•	插件生态平台
	•	面向团队协作的完整产品
	•	一上来就复杂到难以收敛的超级工作台

5.2 第一阶段建议包含的功能

建议先聚焦以下能力：

核心能力
	•	AI 普通聊天
	•	多会话管理
	•	会话的创建、删除、重命名
	•	消息流式展示
	•	Markdown / 代码块渲染
	•	本地持久化
	•	基础设置页
	•	Provider / Model 配置

体验能力
	•	会话搜索
	•	基础快捷键
	•	基础错误提示与失败重试
	•	简单的 prompt 模板或预设

暂不纳入第一阶段
	•	多 Agent 协作
	•	复杂 Skill 编排
	•	云同步
	•	多端同步
	•	插件市场
	•	团队协作
	•	重型权限系统
	•	复杂知识库系统

⸻

6. 推荐技术路线

6.1 桌面端

Electron

当前阶段优先推荐 Electron，原因是：
	•	适合快速落地桌面 AI 工具
	•	与现有前端技术能力匹配度高
	•	本地能力集成成熟
	•	能较顺畅地处理文件系统、系统快捷键、窗口、托盘等桌面能力
	•	更适合作为第一阶段的务实选择

当前阶段不优先追求更小包体或更“纯正”的原生体验，因此暂不把 Tauri 作为首选。

6.2 前端
	•	React
	•	TypeScript
	•	Vite

原则是保持熟悉、直接、迭代快，不引入不必要的复杂度。

6.3 数据存储

SQLite

本地桌面应用非常适合用 SQLite 做第一阶段持久化。

可用于存储：
	•	会话
	•	消息
	•	设置
	•	Provider 配置
	•	Prompt 模板
	•	后续 run / step 等运行记录

6.4 状态管理

UI 层可以使用较轻量的状态管理方案；真正的持久化状态应落入数据库，而不是堆在前端状态库中。

6.5 模型接入

建议一开始就做一层 provider adapter，而不是把某个模型厂商 SDK 直接写死到业务中。

这层抽象至少应支持：
	•	provider 配置
	•	模型配置
	•	普通聊天调用
	•	流式输出
	•	错误统一处理
	•	后续预留 tool / skill 扩展口

⸻

7. 函数式与工程实现的建议

7.1 哪些部分适合函数式优先

建议重点放在这些模块：
	•	domain model
	•	conversation / message / run 的状态迁移
	•	provider 输出标准化
	•	prompt 构造
	•	配置合并
	•	错误建模
	•	schema 定义与解析

这些部分适合做到：
	•	显式输入输出
	•	尽量无副作用
	•	可独立测试
	•	易于 property-based testing

7.2 哪些部分务实处理

以下部分不需要强行函数式化：
	•	Electron 主进程
	•	IPC
	•	窗口管理
	•	文件系统
	•	数据库连接
	•	系统集成能力
	•	UI 细节交互

原则不是“放弃设计”，而是：

把副作用集中在边界层，而不是让副作用污染核心逻辑。

7.3 是否直接使用 Effect

当前建议：
	•	暂不在第一阶段全量引入 Effect
	•	可以吸收其思想
	•	可以在局部尝试某些能力，如 schema 或复杂异步流程建模
	•	以“减少具体复杂度”为前提，而不是因为它看起来更高级

第一阶段的目标是简单、优雅、不过度设计，因此不建议为了体系完整性而给整个项目增加额外学习和组织负担。

⸻

8. 测试策略建议

8.1 测试分层

建议采用以下测试分层：

单元测试

覆盖纯逻辑：
	•	状态迁移
	•	数据转换
	•	配置合并
	•	schema decode / encode
	•	provider 响应标准化

契约测试

覆盖边界接口：
	•	provider adapter
	•	repository
	•	IPC 通信契约
	•	skill / tool 协议

集成测试

覆盖关键流程：
	•	发送消息到流式响应完成
	•	数据落库与恢复
	•	设置变更生效
	•	失败与重试

E2E 测试

仅覆盖核心黄金路径：
	•	新建会话并聊天
	•	切换模型
	•	查看历史会话
	•	删除 / 搜索会话

8.2 fast-check 的使用方向

fast-check 非常适合用于核心逻辑验证，例如：
	•	状态机或状态迁移不变量
	•	provider 事件归一化
	•	repository round-trip
	•	配置合并逻辑
	•	parser / serializer

它应该被用在“边界情况很多、手写样例不够覆盖”的模块，而不是无差别铺满整个项目。

⸻

9. 当前阶段的成功标准

第一阶段成功，不取决于：
	•	抽象有多高级
	•	技术栈有多前沿
	•	代码看起来多像一篇论文

而取决于：

9.1 是否真的可用

Nyx 是否已经成为一个自己愿意真实使用的 AI 桌面工具。

9.2 是否具备持续演进空间

第一阶段完成后，是否能够继续自然扩展，而不是必须推倒重来。

9.3 是否沉淀出高质量工程基础

是否在第一阶段中建立了：
	•	清晰的分层
	•	严格的类型约束
	•	较好的测试策略
	•	可维护的工程节奏

⸻

10. 一句话总结

Nyx 第一阶段不是为了做一个“大而全”的 AI 产品，而是为了做一个：

以自己为核心用户、能真正长期使用、架构克制但质量很高的桌面 AI 聊天工具。

它的核心策略是：
	•	在 scope 上克制
	•	在核心模型上追求函数式与清晰性
	•	在边界层上保持务实
	•	在工程质量上尽量做到严格与扎实
	•	在未来扩展上留有余地，但不提前背负未来的复杂度