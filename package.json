{
  "name": "nyx-workspace",
  "version": "0.1.0",
  "private": true,
  "description": "Nyx workspace.",
  "type": "module",
  "scripts": {
    "prepare": "lefthook install",
    "dev": "mise run desktop:dev",
    "build": "mise run desktop:build",
    "check": "mise run desktop:check",
    "format": "mise run desktop:format",
    "format:check": "mise run desktop:format-check",
    "lint": "mise run desktop:lint",
    "typecheck": "mise run desktop:typecheck",
    "typecheck:compat": "mise run desktop:typecheck:compat"
  },
  "devDependencies": {
    "lefthook": "^2.1.9",
    "oxfmt": "^0.41.0",
    "oxlint": "^1.69.0"
  }
}
